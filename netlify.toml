[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "24"

# Send every URL to the app so deep links (e.g. /records/123) work on refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
